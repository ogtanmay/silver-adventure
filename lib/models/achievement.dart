class Achievement {
  const Achievement({
    required this.title,
    required this.unlocked,
    required this.description,
  });

  final String title;
  final bool unlocked;
  final String description;
}
